Variable representation: anonymized → named → described
Observation representation: raw matrix → statistical summaries → natural-language summaries
SCM family: linear-Gaussian → nonlinear → discrete → mixed
Intervention type: hard single-node → soft → multi-node → conditional
Query type: structure recovery → causal effect estimation → counterfactual → decision under uncertainty
Scoring: F1+SHD → MEC-aware → effect-estimation error → decision regret